version:'3.8'
services:
    web:
      build: ./frontend
      ports:
        - "5173:5173"
      voulumes:
      - .:/app
      - app/node_modules
      environment:
      - CHOKIDAR_USEPOLLING = true